- `torch.export` dynamic-shape handling for variadic keyword arguments belongs to the export worksheet, not ONNX ([#185730](https://github.com/pytorch/pytorch/pull/185730))
- Fix typos in comments and docstrings across torch ([#190827](https://github.com/pytorch/pytorch/pull/190827)) — from quantization; labeled `release notes: distributed (ddp)` / `release notes: distributed (dtensor)`, belongs to distributed.
- [inductor] fix remainder for fp16/bf16 tensors and scalar inputs ([#185168](https://github.com/pytorch/pytorch/pull/185168)) — from quantization; an inductor numerical fix, belongs to inductor.
- Migrate deprecated `torch.norm` calls to `torch.linalg.*` ([#185097](https://github.com/pytorch/pytorch/pull/185097)) — from quantization; cross-cutting internal refactor touching linalg, distributed, nn, and optim, not specifically quantization.
- Fix `multi_margin_loss` decomposition lowering on XPU by using one-dimensional weight indexing ([#188770](https://github.com/pytorch/pytorch/pull/188770)) — from composability; belongs to XPU.
- Fix the AOTAutograd memory-budget partitioner short-circuit so it does not save a non-Tensor tuple node ([#188014](https://github.com/pytorch/pytorch/pull/188014)) — from autograd_frontend; belongs to aotdispatcher.
- Add `C10_LIFETIMEBOUND` to the borrowing constructors of `c10::OptionalArrayRef` ([#190076](https://github.com/pytorch/pytorch/pull/190076)) — from inductor (aoti); the change is confined to `c10/util/OptionalArrayRef.h` and has no AOTInductor content, belongs to cpp_frontend.
- Add hinted symbolic storage size metadata to `FakeTensor` ([#183839](https://github.com/pytorch/pytorch/pull/183839)) — from inductor (aoti); a FakeTensor metadata change, belongs to composability.
- Migrate XPU ATen op registrations to `native_functions.yaml` ([#181233](https://github.com/pytorch/pytorch/pull/181233)) — from inductor (aoti); an ATen op registration change with no AOTInductor content, belongs to xpu.
- Fix partitioner SymInt bindings for backward ([#185473](https://github.com/pytorch/pytorch/pull/185473)) — from inductor (aoti); an AOTAutograd partitioner fix, belongs to aotdispatcher. (Was listed twice in the aoti worksheet.)
- Remove the deprecated C++ `c10::Scalar::isIntegral()` and `c10::isIntegralType(ScalarType)` overloads that omit `includeBool` ([#187115](https://github.com/pytorch/pytorch/pull/187115)) — from JIT; belongs to C++ frontend.
- Remove the deprecated `torch.cholesky` API in favor of `torch.linalg.cholesky` ([#186817](https://github.com/pytorch/pytorch/pull/186817)) — from mobile; BC-breaking change that belongs to linalg frontend.
- Remove the deprecated `torch.qr` API in favor of `torch.linalg.qr` ([#186815](https://github.com/pytorch/pytorch/pull/186815)) — from mobile; BC-breaking change that belongs to linalg frontend.
- Add post-allocation CUDA memory annotations to allocator snapshots and memory visualizations ([#190575](https://github.com/pytorch/pytorch/pull/190575)) — from python_frontend; belongs to CUDA.
- Remove an extra clone from compiled stateless RNG operations ([#188495](https://github.com/pytorch/pytorch/pull/188495)) — from python_frontend; belongs to inductor.
- Skip the copy-on-write `histogramdd` test on XPU where its prerequisite operator is unavailable ([#174670](https://github.com/pytorch/pytorch/pull/174670)) — from python_frontend; belongs to XPU.
- Fix assignment of Python integers larger than `INT64_MAX` into `torch.uint64` tensors ([#191604](https://github.com/pytorch/pytorch/pull/191604)) — from python_frontend; belongs to indexing.
- Relax tolerances for nondeterministic XPU convolution and histogram tests ([#177069](https://github.com/pytorch/pytorch/pull/177069)) — from python_frontend; belongs to XPU.
- Use unique values in CUDA/CuteDSL `topk` OpInfo samples to avoid ambiguous tie ordering ([#188838](https://github.com/pytorch/pytorch/pull/188838)) — from python_frontend; belongs to CUDA.
- Add a curried decorator calling convention for eager `torch.utils.checkpoint.checkpoint()` ([#189411](https://github.com/pytorch/pytorch/pull/189411)) — from python_frontend; checkpointing is an autograd feature and belongs to autograd_frontend.
- Add `respect_saved_tensors_hooks` to selective activation checkpointing and warn before its default behavior changes ([#190581](https://github.com/pytorch/pytorch/pull/190581)) — from python_frontend; checkpointing is an autograd feature and belongs to autograd_frontend.
- Generalize distributed test utilities to run on Intel GPUs ([#178468](https://github.com/pytorch/pytorch/pull/178468)) — from inductor; the production and test changes primarily exercise sharded distributed code, so route to distributed (sharded).
- Correct grammatical errors in distributed comments, docstrings, and documentation ([#187361](https://github.com/pytorch/pytorch/pull/187361)) — from inductor; the release-note label and substantive files are distributed (c10d).
- Preserve third-party backend configuration and extra Triton imports in generated minifier repros ([#187855](https://github.com/pytorch/pytorch/pull/187855)) — from inductor; all production changes are in Dynamo repro generation, so route to dynamo.
- Update the Triton CPU pin and migrate CPU block-pointer coverage to tensor descriptors ([#188794](https://github.com/pytorch/pytorch/pull/188794)) — from inductor; the dependency pin and backend migration are release-engineering work, so route to releng.
- Register installed `target_code` with Dynamo's package input tracker so `torch._dynamo.reset()` clears stale precompiled entries ([#189206](https://github.com/pytorch/pytorch/pull/189206)) — from inductor; the implementation is confined to Dynamo packaging, so route to dynamo.
- Correct duplicated words in distributed comments and a warning string ([#190035](https://github.com/pytorch/pytorch/pull/190035)) — from inductor; the release-note label and relevant runtime text belong to distributed (c10d).
- Restore ROCm Inductor OpInfo coverage for reverse power and interpolation ([#191534](https://github.com/pytorch/pytorch/pull/191534)) — from inductor; this is Python-frontend test coverage and is labeled for python_frontend.
- Support unsigned 16-, 32-, and 64-bit index expressions in Inductor's Metal code generation ([#192020](https://github.com/pytorch/pytorch/pull/192020)) — from inductor; the behavior is specific to MPS and belongs in the mps worksheet.
- Make some aspects of Dynamo and Inductor global state thread-safe ([#168999](https://github.com/pytorch/pytorch/pull/168999)) — from inductor; the thread-local FX tracing flag and related synchronization belong in the fx worksheet.
- Lazily register CUDA Graph generator state and deprecate `CUDAGraph.register_generator_state()` ([#176753](https://github.com/pytorch/pytorch/pull/176753)) — from inductor; this public CUDA Graph deprecation and RNG-state lifetime fix belong in the cuda worksheet.
- Add the `simple_overlap` scheduler pass for communication/computation overlap ([#184235](https://github.com/pytorch/pytorch/pull/184235)) — from inductor; this is one stack with #184240 and belongs with compiled distributed scheduling in distributed (c10d).
- Enable `simple_overlap` by default for compiled distributed workloads ([#184240](https://github.com/pytorch/pytorch/pull/184240)) — from inductor; this changes distributed collective scheduling and belongs in distributed (c10d).
- Make generated kernel-context metadata opt-in ([#184513](https://github.com/pytorch/pytorch/pull/184513)) — from inductor; the behavior is specific to AOTInductor's C++ wrapper and belongs in inductor (aoti).
- Add XPU support for asynchronous tensor parallelism through XCCL and symmetric memory ([#185102](https://github.com/pytorch/pytorch/pull/185102)) — from inductor; the fused symmetric-memory operations belong in distributed (symm_mem).
- Fix AOTInductor CUDA multi-architecture target packaging ([#185328](https://github.com/pytorch/pytorch/pull/185328)) — from inductor; the packaging and embedded-kernel fix belong in inductor (aoti).
- Fix AOTInductor integer-symbol recovery from composed dynamic sizes ([#185841](https://github.com/pytorch/pytorch/pull/185841)) — from inductor; the generated C++ wrapper fix belongs in inductor (aoti).
- Add Inductor kernel provenance to exported profiler timelines ([#186230](https://github.com/pytorch/pytorch/pull/186230)) — from inductor; the release-note label routes this profiler/FX provenance feature to fx.
- Add optional pinned asynchronous host-to-device copies for AOTInductor constant loading ([#186258](https://github.com/pytorch/pytorch/pull/186258)) — from inductor; the constant-loading runtime path belongs in inductor (aoti).
- Avoid a CUDA synchronization when `torch.normal` validates tensor-valued standard deviations ([#186508](https://github.com/pytorch/pytorch/pull/186508)) — from inductor; the eager CUDA validation and CUDA Graph behavior change belong in cuda.
- Fail fast when loading a CUDA AOTInductor package without CUDA availability ([#186943](https://github.com/pytorch/pytorch/pull/186943)) — from inductor; the package-loader error handling belongs in inductor (aoti).
- Fix AOTInductor C++ wrapper fallback outputs and tensor-alignment metadata ([#187331](https://github.com/pytorch/pytorch/pull/187331)) — from inductor; both fixes are specific to the AOTInductor C++ wrapper.
- Support integer-list arguments in AOTI eager cache keys ([#187360](https://github.com/pytorch/pytorch/pull/187360)) — from inductor; the AOTI eager dispatch cache belongs in inductor (aoti).
- Correct typos in quantization and related documentation ([#187367](https://github.com/pytorch/pytorch/pull/187367)) — from inductor; the release-note label routes this documentation-only change to quantization.
- Fix AOTInductor C++ wrapper handling for Python-only graph inputs ([#188030](https://github.com/pytorch/pytorch/pull/188030)) — from inductor; the input-slot fix belongs in inductor (aoti).
- Support fallback operators with `Any` arguments in the AOTInductor C++ wrapper ([#188124](https://github.com/pytorch/pytorch/pull/188124)) — from inductor; the boxed fallback path belongs in inductor (aoti).
- Route custom operators with symbolic scalar arguments through boxed AOTInductor dispatch ([#188154](https://github.com/pytorch/pytorch/pull/188154)) — from inductor; the C++ wrapper fix belongs in inductor (aoti).
- Box absent non-optional tensor arguments as undefined tensors in the C++ wrapper ([#188485](https://github.com/pytorch/pytorch/pull/188485)) — from inductor; this AOTInductor custom-operator fix belongs in inductor (aoti).
- Add an AOTInductor C API for caller-owned external constants and zero-copy weight sharing ([#188643](https://github.com/pytorch/pytorch/pull/188643)) — from inductor; this public AOTInductor runtime feature belongs in inductor (aoti).
- Fix compiled `torch.signbit` for `float64` inputs on XPU ([#188818](https://github.com/pytorch/pytorch/pull/188818)) — from inductor; the backend-specific correctness fix belongs in xpu.
- Correct typos in FX comments, docstrings, and diagnostics ([#188870](https://github.com/pytorch/pytorch/pull/188870)) — from inductor; the release-note label routes this internal text-only change to fx.
- Add internal helpers for retrieving real tensors, devices, and constants from fake tensors ([#188975](https://github.com/pytorch/pytorch/pull/188975)) — from inductor; the fake-tensor/export plumbing belongs in export.
- Correct comments, docstrings, and log-message typos across Inductor and distributed code ([#190039](https://github.com/pytorch/pytorch/pull/190039)) — from inductor; labels route this internal-only change to inductor (aoti) and distributed (c10d).
- Correct typographical errors in quantization-related comments and documentation ([#190398](https://github.com/pytorch/pytorch/pull/190398)) — from inductor; the release-note label routes this internal-only change to quantization.
- Correct comments and docstrings across C++ frontend and DTensor code ([#190434](https://github.com/pytorch/pytorch/pull/190434)) — from inductor; the release-note labels route this text-only change to cpp_frontend and distributed (dtensor).
- Fix a null tensor handle when an AOTInductor fallback operator returns a one-element `Tensor[]` ([#190551](https://github.com/pytorch/pytorch/pull/190551)) — from inductor; the C++ wrapper crash fix belongs in inductor (aoti).
- Correct comments and docstrings across export and DTensor modules ([#190569](https://github.com/pytorch/pytorch/pull/190569)) — from inductor; the release-note labels route this text-only change to export and distributed (dtensor).
- Respect runtime-assert bounds when deriving hints for unbacked symbolic sizes ([#190589](https://github.com/pytorch/pytorch/pull/190589)) — from inductor; the size-hinting implementation and release-note label belong in fx.
- Add CUDA compute capability 10.7 (`sm_107`) support for Rubin GPUs ([#190654](https://github.com/pytorch/pytorch/pull/190654)) — from inductor; CUDA architecture and extension-build support belong in cuda.
- Correct comments and docstrings across quantization and distributed checkpoint code ([#190831](https://github.com/pytorch/pytorch/pull/190831)) — from inductor; the release-note labels route this internal-only change to quantization and distributed (checkpoint).
- Emit portable array pointers in AOTInductor CPU wrappers ([#191240](https://github.com/pytorch/pytorch/pull/191240)) — from inductor; the generated C++ portability fix belongs in inductor (aoti).
- Fix lint violations in Inductor and FX unification code ([#191866](https://github.com/pytorch/pytorch/pull/191866)) — from inductor; this internal-only cleanup is labeled for fx.
